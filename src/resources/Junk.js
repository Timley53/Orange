  // const [failedCharacterUsername, setfailedCharacterUsername] = useState(false)
 
   // const [username, setUsername] = useState('')

 
   {/* <label htmlFor="UserName" className='my-3 '>
          <h3>Username:</h3>
          <input type="text"
           value={username} 
           onChange={(e)=>setUsername(e.target.value)}

           onFocus={()=> setfailedCharacterUsername(false)}

           className={`border-4 ${failedCharacterUsername ? 'border-red-600': ''} border-orange-200 w-[90%] text-sm  rounded md:w-full`}  required/>

          {failedCharacterUsername && <small>Username must be more than 4 characters and must contain upppercase and no special characters($,*#)</small>}
      </label> */}

 
 
 
 
 
 /* try{
      
      //show spinner
      setShowSPinner(!showSpinner)
      // const userCredentials = await createUserWithEmailAndPassword(auth,email, password)

      // console.log(userCredentials);
       


      createUserWithEmailAndPassword(auth,email, password).then((userCredentials)=>{
        setShowSPinner(!showSpinner)
        console.log(userCredentials);



    }).catch(err =>{ 
      
      console.log(err.message)
      setfailedCharacterPassword(err.message.split(' ').slice(1).join(' '))
      setShowSPinner(false)
      
    })
      

    
    
      // if(!characterTest(username) || username.length < 4){
      //   throw new Error('username')
      //   return
      // }
      
      // if(!characterTest(password) || password.length < 4){
      //   throw new Error('password')
      //   return
      // }

      
      // console.log(email,password);
      // dispatch(updateUserDetails({
      //   //  username,
      //   password,
      //   email
      // }))
      // console.log(userStore.getState());
      // setSignedUp(true)
      // CallTimer()
      // callTimer(timer,setSignedUp)

// setShowSPinner(true)

    }catch(err){

      // setfailedCharacterPassword(true);



    }
    */